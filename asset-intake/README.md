# Asset Intake

Drop site asset packages here for inclusion in the K&D Auto Electric site.

Expected package: `kd-site-image-package.zip` (building/sign photos, commercial video, membership logos).

Process: place the zip in this folder → commit → push to main → extract/process assets → wire into `src/`.
